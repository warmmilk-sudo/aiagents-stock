[POSITION] 当前持仓（$stock_code） ⭐ 重要
═══════════════════════════════════════════════════════════
持仓日期: $position_date
持仓天数: $holding_days_text
今日可卖: $can_sell_today_text
持仓数量: $position_quantity股
成本价: $position_cost
当前价: $current_price
持仓市值: $current_total
浮动盈亏: $profit_loss_text
ATR14: $atr14_text
ATR波动率: $atr14_pct_text
系统ATR防守底线: $atr_stop_floor_text
跟踪均线锚点: $trend_anchor_text
量化信标: $feature_beacons_text

💡 决策建议：
- 若基线已明确波段类型，优先按 `$swing_type_label`（$swing_horizon_days_text）视角管理；若未明确，只按持仓成本、结构强弱和风险约束执行，不自动套固定波段模板。
- 基线风格：$strategy_style_summary
- 基线退出方式：$baseline_exit_style
- 你必须在本轮 JSON 中自行判定 `structure_state`，只能四选一：`宽幅震荡洗盘` / `趋势突破确认` / `主升加速段` / `筑顶高位派发`
- 优先区分 `回踩确认加仓`、`突破确认加仓`、`主动减仓锁盈`、`防守减仓/清仓` 这几类动作，而不是只看涨跌。
- 先围绕成本价和当前浮盈亏判断，不要忽略成本锚。
- 若“今日可卖”为“否（T+1限制）”，本次只能给出 `HOLD`，不得建议卖出。
- 如果 `15/30/60分钟` 持续走坏、关键位失守、承接恶化，且亏损超过止损线（通常-$stop_loss_pct%）→ 建议止损卖出
- 如果只是短线波动或瞬时下探，但 `30/60分钟` 结构未坏 → 不要轻易把正常回撤当作止损信号
- 如果风险在扩大但趋势未完全坍塌 → 可先考虑 `防守减仓`，不必一上来就 `清仓`
- 如果价格接近基线止盈位但 `60分钟` 结构未走坏、承接仍在 → 优先继续持有或上修止盈观察位，不必主动锁盈
- 如果价格已接近或进入基线/动态止盈区 → 先看 `60分钟` 趋势和量能，再用短时量能（如15/30分钟、5分钟）寻找更好的卖出点，而不是机械先减仓
- 如果盈利较多但趋势仍强，只想兑现部分利润 → 优先考虑 `主动减仓锁盈`，而不是直接清仓
- 若当前是 `标准波段`，且客观条件满足“持仓>=10日 + 利润垫充足 + 未跌破跟踪均线 + 非高位派发”，可考虑进入 `无限趋势跟随期`
- 如果趋势重新走强、`60分钟` 结构先修复，回踩确认有效，且短时量能配合 → 可考虑小到中等比例 `回踩确认加仓`
- 如果关键压力位被有效突破并站稳，且 `60分钟` 与短时量能继续共振 → 可考虑小到中等比例 `突破确认加仓`
- 若 `量化信标` 已点亮，且当前基线仍是 `微波段`，可以评估是否授权升级到 `标准波段`；若升级，必须输出 `swing_type_upgrade=true`
- 如果技术指标强势且未到止盈位 → 建议继续持有
- 如果选择 `加仓`，要注意当日新增仓位同样受 `T+1` 限制，不能当日卖出
