[project]
name = "aiagents-stock"
version = "1.0.0"
description = "复合多AI智能体股票团队分析系统"
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
addopts = "-v --tb=short"
